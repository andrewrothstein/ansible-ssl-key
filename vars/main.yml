---
# vars file for ssl-key
pki_key_file : '{{inventory_hostname}}{{pki_key_suffix}}'
pki_key : '{{pki_dir}}/{{pki_key_file}}'

pki_cert_file : '{{inventory_hostname}}{{pki_cert_suffix}}'
pki_cert : '{{pki_dir}}/{{pki_cert_file}}'
